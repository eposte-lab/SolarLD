{{ copy_subject | default(subject, true) }}

{{ business_name | default(greeting_name, true) }}, il vostro tetto{% if hq_province %} a {{ hq_province }}{% endif %} può produrre {% if roi and roi.get('yearly_kwh') %}{{ roi.yearly_kwh | int | format_money }} kWh all'anno{% else %}energia pulita dall'anno prossimo{% endif %}.

{{ copy_opening_line | default('Abbiamo analizzato la vostra sede via satellite. Qui sotto la sintesi; il report completo con simulazione, piano incentivi e ROI è nella vostra area personale.', true) }}
{% if epc_enabled %}
MODALITÀ EPC — FORMULA CHIAVI IN MANO
Progettazione, fornitura e installazione: dalla A alla Z, le seguiamo noi.
E sul «come si paga» c'è una formula che cambia le carte in tavola: i numeri, calcolati sul vostro tetto, sono nel report qui sotto.
{% endif %}
{% if roi and (roi.get('yearly_kwh') or roi.get('yearly_savings_eur')) -%}
I NUMERI PRINCIPALI:
{% if roi.get('yearly_kwh') %}  Producibilità annua:        {{ roi.yearly_kwh | int | format_money }} kWh
{% endif %}{% if roi.get('yearly_savings_eur') %}  Risparmio annuo in bolletta: €{{ roi.yearly_savings_eur | int | format_money }}
{% endif %}
{% endif -%}
{{ tenant_legal_name | default(tenant_name, true) }} · Installatore certificato GSE

La nostra proposta — percorso completo chiavi in mano:
- Sopralluogo tecnico entro 7 giorni
- Analisi incentivi 2026 (Transizione 5.0, CER, autoconsumo)
- Pratica GSE gestita in meno di 30 giorni, dalla progettazione all'allaccio

Vedi il report del vostro tetto:
{{ lead_url }}
(Accesso al report personale · Nessuna registrazione richiesta)

--
A disposizione, {{ email_signature | default(sender_first_name | default(tenant_name, true), true) }}
{{ tenant_name }}

---
{{ tenant_legal_name | default(tenant_name, true) }}{% if tenant_vat_number %} · P.IVA {{ tenant_vat_number }}{% endif %}{% if tenant_legal_address %} · {{ tenant_legal_address }}{% endif %}
Email B2B (art. 6.1.f GDPR — legittimo interesse). Per disiscriverti: {{ optout_url }}
