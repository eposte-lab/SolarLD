{{ headline | default('Tre numeri che cambiano la prospettiva', true) }}

Per {{ greeting_name }},

{% if sector_news_headline -%}
{{ sector_news_headline }}.

{{ sector_news_body }}
{% else -%}
{{ copy_opening_line | default('Vi scrivo per condividere tre dati semplici sull\'autoconsumo energetico.', true) }}

{{ main_copy_1 | default('Le aziende italiane simili a voi che hanno installato il fotovoltaico nell\'ultimo anno hanno ridotto la spesa energetica tra il 30% e il 60%, con un rientro medio di 5 anni.', true) }}
{% endif %}
{% if roi and roi.yearly_savings_eur is defined -%}
La vostra stima personalizzata: €{{ roi.yearly_savings_eur | int | format_money }}/anno di risparmio{% if roi.payback_years %} (rientro ~{{ roi.payback_years }} anni){% endif %}.

{% endif -%}
{{ copy_proposition_line | default('Se preferite scriverci direttamente, basta rispondere a questa email.', true) }}

Analisi completa: {{ lead_url }}

---
{{ sender_first_name | default(tenant_name, true) }} — {{ tenant_name }}

{{ tenant_legal_name | default(tenant_name, true) }}{% if tenant_vat_number %} · P.IVA {{ tenant_vat_number }}{% endif %}.
Email B2B (art. 6.1.f GDPR). Annulla: {{ optout_url }}
