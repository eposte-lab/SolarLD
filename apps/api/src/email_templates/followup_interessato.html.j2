{# Engagement-based follow-up — INTERESSATO scenario (score 41-60).
   Tone: case study + concrete next step. Lead is clearly interested
   but not yet hot. We invite them to a 15-min call. #}
<!DOCTYPE html>
<html lang="it">
<head>
<meta charset="UTF-8">
<meta name="viewport" content="width=device-width, initial-scale=1.0">
<title>{{ headline | default('Una proposta concreta per ' + (business_name or 'la vostra sede'), true) }}</title>
</head>
<body style="margin:0; padding:0; background-color:#f5f2eb; font-family:-apple-system,BlinkMacSystemFont,'Segoe UI',Arial,sans-serif;">
<div style="display:none; max-height:0; overflow:hidden; mso-hide:all; font-size:1px; color:#f5f2eb;">
  Una conversazione di 15 minuti per chiudere i numeri sul fotovoltaico.
</div>
<table role="presentation" border="0" cellpadding="0" cellspacing="0" width="100%" style="background-color:#f5f2eb;">
  <tr><td align="center" style="padding:24px 12px;">
    <table role="presentation" border="0" cellpadding="0" cellspacing="0" width="600" style="max-width:600px; background:#fff; border-radius:8px; overflow:hidden;">
      <tr>
        <td style="padding:20px 32px; background-color:{{ brand_primary_color | default('#0B3D2E', true) }};">
          {%- if brand_logo_url %}
          <img src="{{ brand_logo_url }}" alt="{{ tenant_name }}" width="140" style="max-height:40px; width:auto; display:block;">
          {%- else %}
          <span style="color:#fff; font-size:15px; font-weight:700;">{{ tenant_name }}</span>
          {%- endif %}
        </td>
      </tr>
      <tr>
        <td style="padding:32px 36px 28px;">
          <p style="margin:0 0 8px; font-size:12px; color:#6b6b6b; text-transform:uppercase; letter-spacing:0.5px;">
            Proposta concreta · per {{ greeting_name }}
          </p>
          <h1 style="margin:0 0 18px; font-size:24px; line-height:30px; font-weight:700; color:#0b3d2e;">
            {{ headline | default('Possiamo chiudere i numeri in 15 minuti', true) }}
          </h1>
          <p style="margin:0 0 18px; font-size:15px; line-height:23px; color:#2b2b2b;">
            {{ copy_opening_line | default('Vi rispondo con un&apos;idea pratica: blocchiamo 15 minuti in agenda per rivedere insieme i numeri della vostra simulazione e capire se ha senso passare alla fase di sopralluogo.', true) }}
          </p>
          <p style="margin:0 0 22px; font-size:14px; line-height:22px; color:#444;">
            {{ main_copy_1 | default('Un&apos;azienda simile alla vostra (stesso settore, stessa fascia di consumi) ha installato un impianto da ~80 kWp lo scorso autunno e dopo 4 mesi sta vedendo i risultati attesi sul ROI.', true) }}
          </p>
          {%- if roi and roi.yearly_savings_eur is defined %}
          <div style="background:{{ brand_primary_color | default('#0B3D2E', true) }}; padding:18px 22px; border-radius:6px; margin:0 0 22px; color:#fff;">
            <p style="margin:0 0 4px; font-size:11px; color:rgba(255,255,255,0.7); text-transform:uppercase; letter-spacing:0.6px;">La vostra stima</p>
            <p style="margin:0; font-size:30px; line-height:36px; font-weight:700;">€{{ roi.yearly_savings_eur | int | format_money }}<span style="font-size:15px; font-weight:400; color:rgba(255,255,255,0.7);">/anno</span></p>
            {%- if roi.payback_years %}<p style="margin:6px 0 0; font-size:13px; color:rgba(255,255,255,0.85);">Rientro ~{{ roi.payback_years }} anni · CO₂ evitata ~{{ roi.co2_tonnes_25_years | default(40) }}t in 25 anni</p>{%- endif %}
          </div>
          {%- endif %}
          <table role="presentation" border="0" cellpadding="0" cellspacing="0" width="100%" style="margin:0 0 18px;">
            <tr><td align="center">
              <table role="presentation" border="0" cellpadding="0" cellspacing="0">
                <tr><td style="background-color:{{ brand_color_accent | default('#F4A300', true) }}; border-radius:6px;">
                  <a href="{{ lead_url }}" target="_blank" style="display:inline-block; padding:16px 32px; font-size:15px; font-weight:700; color:#0b3d2e; text-decoration:none;">
                    {{ cta_primary_label | default('Prenota 15 minuti', true) }} &rarr;
                  </a>
                </td></tr>
              </table>
            </td></tr>
          </table>
          <p style="margin:0; font-size:14px; line-height:22px; color:#2b2b2b;">
            {{ copy_proposition_line | default('Se preferite, basta rispondere con due o tre orari comodi e organizziamo noi.', true) }}
          </p>
        </td>
      </tr>
      <tr>
        <td style="padding:0 36px 28px; border-top:1px solid #e8e3d4;">
          <p style="margin:22px 0 4px; font-size:14px; color:#2b2b2b;">{{ sender_first_name | default(tenant_name, true) }}</p>
          <p style="margin:0; font-size:13px; color:#6b6b6b;">{{ tenant_name }}</p>
        </td>
      </tr>
      <tr>
        <td style="padding:18px 36px; background:#f5f2eb;">
          <p style="margin:0 0 6px; font-size:10px; line-height:14px; color:#888;">
            {{ tenant_legal_name | default(tenant_name, true) }}{% if tenant_vat_number %} · P.IVA {{ tenant_vat_number }}{% endif %}{% if tenant_legal_address %} · {{ tenant_legal_address }}{% endif %}
          </p>
          <p style="margin:0; font-size:10px; line-height:14px; color:#888;">
            Email B2B per legittimo interesse (art. 6.1.f GDPR). <a href="{{ optout_url }}" style="color:#888;">Annulla iscrizione</a>
          </p>
        </td>
      </tr>
    </table>
  </td></tr>
</table>
{%- if tracking_pixel_url %}<img src="{{ tracking_pixel_url }}" width="1" height="1" alt="" style="display:block;width:1px;height:1px;border:0;">{%- endif %}
</body></html>
